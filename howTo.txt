## How to use github and create react app

1. on local terminal `create-react-app newFolder`
2. `cd newFolder` then `git init`
3. on Github create new Repo
4. Finaly on local terminal `git remote add origin https://github.com/USER/newFolder.git` and `git push -u origin master`
